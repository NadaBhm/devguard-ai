# DevGuard AI - Development-only dependencies
# (production deps live in requirements.txt)

pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
httpx>=0.27          # for testing FastAPI endpoints
ruff>=0.4
mypy>=1.10
