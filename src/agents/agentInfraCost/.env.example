# Optional. Enables real Gemini-powered explanatory text in llm_enrichment.py
# (architecture_explanation / cost_summary / finops_justification).
# If unset, the agent automatically falls back to deterministic template
# text built from the already-computed decision/cost/finops values — the
# pipeline runs end-to-end either way, with no blocking and no error.
GEMINI_API_KEY=
